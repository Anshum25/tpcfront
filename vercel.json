{
  "installCommand": "npm install && npm rebuild vite",
  "buildCommand": "chmod +x ./node_modules/.bin/vite && npx vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}